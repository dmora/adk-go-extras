# Examples

This directory is reserved for runnable examples that demonstrate concrete
packages from `adk-go-extras`.

Guidelines:

- keep examples small and focused
- prefer one example per package or integration
- avoid turning examples into long-lived sample applications
- document any required environment variables in the example directory
